function y --wraps='podman run -it --rm --name arch-disposable -v "$HOME:$HOME" -w "$HOME" -e LANG=$LANG archlinux:latest sh -c "pacman -Sy && bash"' --wraps='podman run -it --rm --name arch-disposable -v "$HOME:$HOME" -w "$HOME" -e LANG=$LANG archlinux:latest sh -c "pacman -Sy --noconfirm base-devel git && useradd -m -u $(id -u) -G wheel archuser && echo \\"archuser ALL=(ALL) NOPASSWD: ALL\\" >> /etc/sudoers && su - archuser -c \\"cd /tmp && git clone https://aur.archlinux.org/paru-bin.git && cd paru-bin && makepkg -si --noconfirm\\" && exec su - archuser -c \\"cd $PWD && bash\\""' --wraps='podman run -it --rm --name arch-disposable --userns=keep-id -v "$HOME:$HOME" -w "$PWD" -e LANG=$LANG archlinux:latest sh -c "pacman -Sy --noconfirm base-devel git && cd /tmp && git clone https://aur.archlinux.org/paru-bin.git && cd paru-bin && makepkg -si --noconfirm --asdeps && echo \\"allowroot\\" >> /etc/paru.conf && exec bash"' --wraps='doas -s; podman run -it --rm --name arch-disposable --userns=keep-id -v "$HOME:$HOME" -w "$PWD" -e LANG=$LANG archlinux:latest sh -c "pacman -Sy --noconfirm base-devel git && cd /tmp && git clone https://aur.archlinux.org/paru-bin.git && cd paru-bin && makepkg -si --noconfirm --asdeps && echo \\"allowroot\\" >> /etc/paru.conf && exec bash"' --wraps='doas podman run -it --rm --name arch-disposable --userns=keep-id -v "$HOME:$HOME" -w "$PWD" -e LANG=$LANG archlinux:latest sh -c "pacman -Sy --noconfirm base-devel git && cd /tmp && git clone https://aur.archlinux.org/paru-bin.git && cd paru-bin && makepkg -si --noconfirm --asdeps && echo \\"allowroot\\" >> /etc/paru.conf && exec bash"' --description 'alias y=doas podman run -it --rm --name arch-disposable --userns=keep-id -v "$HOME:$HOME" -w "$PWD" -e LANG=$LANG archlinux:latest sh -c "pacman -Sy --noconfirm base-devel git && cd /tmp && git clone https://aur.archlinux.org/paru-bin.git && cd paru-bin && makepkg -si --noconfirm --asdeps && echo \\"allowroot\\" >> /etc/paru.conf && exec bash"'
    doas podman run -it --rm --name arch-disposable --userns=keep-id -v "$HOME:$HOME" -w "$PWD" -e LANG=$LANG archlinux:latest sh -c "pacman -Sy --noconfirm base-devel git && cd /tmp && git clone https://aur.archlinux.org/paru-bin.git && cd paru-bin && makepkg -si --noconfirm --asdeps && echo \"allowroot\" >> /etc/paru.conf && exec bash" $argv
end
